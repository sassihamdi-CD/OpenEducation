package Backend.OpenEducation.Model;

/**
 * Represents the types of educational materials in the OpenEducation system.
 * Each material type corresponds to a specific format (e.g., video, PDF).
 */
public enum MaterialType {

    // Represents video materials
    VIDEO,
    // Represents PDF documents
    PDF,
}
